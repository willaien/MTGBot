namespace DiscordBot
{
    class Program
    {
        static void Main(string[] args)
        {
            var bot = new Bot();
            bot.Start(); //Starts and waits so that we don't exit out.
        }
    }

 }
